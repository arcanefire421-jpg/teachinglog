# 專案功能紀錄

## 整合版工作台

更新日期：2026-07-19

主頁 `index.html` 已整合為單一入口，包含三個分頁：

1. 上課進度
2. 快速製作檔名
3. 課程資料

### 整合內容

- 上課進度與檔名工具共用 `course-data.json`。
- 課程資料檢查功能已整合進主頁，不必另外開 `course-check.html`。
- 課程資料分頁可篩選科目、冊別 / 教材，搜尋章、節、小重點。
- 課程資料分頁可展開 / 收合章節、複製目前路徑、匯出本科 CSV。
- 課程資料分頁保留待修正清單，可標記章、節、小重點並匯出 CSV。
- 在課程資料章節清單點「進度」可直接加入今日進度並切回進度分頁。
- 選擇課程資料分頁的科目與教材時，會同步到上課進度與檔名工具。

### 已修改檔案

- `index.html`
- `app.js`
- `styles.css`
- `project-features.md`

### 已驗證

- `course-data.json` 可被 JSON parser 正常解析。
- `app.js` 已用 Codex 內建 Node.js 通過語法檢查。
- 本機 HTTP 伺服器 `http://localhost:8765/` 可讀取 `index.html` 與 `course-data.json`，回應狀態皆為 `200`。

## 彩色圖片輸出版

更新日期：2026-07-19

主頁的日誌預覽已從純文字改為結構化彩色卡片，讓「另存圖片」輸出的 PNG 更容易閱讀。

### 設計重點

- 複製功能仍保留原本純文字格式。
- 圖片預覽改用卡片式版面，包含班級、日期、科目、班型與輸出模式。
- 今日進度、頁數、小考、作業使用不同色彩區塊。
- LINE 緊實、正式紀錄、給家長版會使用不同視覺氛圍。
- 手機版會自動改成單欄，避免資訊擠壓。

### 色彩規則

- 進度：藍色系
- 頁數：紫色系
- 小考：橘色系
- 作業：綠色系
- 家長提醒：珊瑚粉色系

### 已修改檔案

- `app.js`
- `styles.css`
- `project-features.md`

## 圖片尺寸版型化

更新日期：2026-07-19

圖片尺寸選單已從單純調整輸出寬高，升級為會同步影響預覽版型。

### 版型行為

- 圖片自動：維持一般彩色卡片。
- LINE 貼文：改為較緊湊的資訊卡，適合聊天或貼文快速閱讀。
- IG 直式：改為直式海報感，標題更大、單欄資訊區塊、留白較充足。
- A4 列印：改為較正式的白底紀錄表樣式，降低裝飾色彩。

### 技術重點

- `imageSize` 切換時會即時更新 `#preview` 的 size class。
- 另存圖片仍會依選擇套用實際輸出寬高。
- 複製文字不受圖片版型影響。

## 原版功能補回與操作優化

更新日期：2026-07-19

依照 GitHub 原版 `arcanefire421-jpg/teachinglog` 比對結果，已先補回高頻工作流。

### 新增 / 補回

- 明天快速按鈕。
- 進度暫存區，可先收集章節後再加入今日進度。
- 章節與搜尋結果旁新增 `+`，可直接存入暫存。
- 產生日誌時會自動把暫存區進度併入今日進度並清空暫存。
- 進度搜尋、最近存檔搜尋、資料搜尋新增清除按鈕。
- 班級模板新增單筆刪除，不必清空全部模板。
- 作業片語改為可自訂句庫，可新增、套用、刪除。

### 已驗證

- 明天按鈕可設定日期為隔日。
- 目前章節可存入暫存，且只新增一筆目前路徑。
- 暫存區可清空、套用到今日進度。
- 自訂作業片語可新增、套用、刪除。
- 班級模板可單筆新增後刪除。
- 三個搜尋清除按鈕有效。
- `app.js` 通過語法檢查，且沒有缺漏 HTML ID。

## 最近存檔管理強化

更新日期：2026-07-19

依照原版存檔管理功能補回常用操作。

### 新增 / 補回

- 最近存檔可依日期、班級、科目分組。
- 最近存檔新增班級篩選。
- 最近存檔新增收藏與取消收藏。
- 可切換只看收藏。
- 每筆存檔可下載為 `.txt` 文字檔。
- 收藏存檔以淡黃色卡片標示。

### 已驗證

- 建立測試存檔後可依班級分組。
- 班級篩選可只顯示指定班級。
- 收藏後只看收藏可正確顯示。
- 文字檔按鈕可觸發下載流程且無 console error。
- 測試存檔已刪除，最近存檔沒有殘留測試資料。

## 資料品質掃描

更新日期：2026-07-19

課程資料分頁已補上原版的資料檢查思路，讓合併後的 `course-data.json` 可直接在主頁做品質檢查。

### 新增 / 補回

- 可檢查目前科目。
- 可全科掃描。
- 可產生合併摘要，找出跨科目 / 跨教材可能重複或可合併的章節資料。
- 可清除檢查結果，不影響課程資料本身。
- 檢查結果會顯示掃描科目數、問題總數、隱藏筆數。
- 問題項目以標籤區分類型，並顯示對應的科目、教材、章節路徑。

### 檢查重點

- 空白或格式可疑的章節、節次、小重點名稱。
- 過短、過長或含異常符號的資料名稱。
- 同一教材內可能重複的章節 / 節次。
- 不同科目或教材間可能可合併的相似書名。

### 已驗證

- 目前科目檢查可正常輸出結果。
- 全科掃描可正常輸出結果。
- 合併摘要可正常輸出結果。
- 清除按鈕可清空檢查結果。
- localhost 頁面執行時沒有 console error。

## 快速製作檔名

更新日期：2026-07-19

此功能位於主頁 `index.html` 的「快速製作檔名」區塊，主要用來依照既有課程資料快速產生教材檔名，避免手動輸入造成格式不一致。

### 主資料來源

- 使用 `course-data.json` 作為科目、課程 / 冊別、章節資料來源。
- 不另外建立章節清單。
- 課程名稱可由系統自動帶入，也可透過「課程名稱微調」欄位調整成較適合檔名的名稱。

### 支援類型

1. 自編教材
2. 模擬考
3. 段考
4. 其他文件

### 檔名規則

自編教材：

```text
課程_章節_單元_文件名稱_版本_狀態
```

範例：

```text
選修物理1_CH02_運動學_講義01_教用_草稿
選修物理1_CH02_運動學_小考01_學用_完稿
```

模擬考：

```text
課程_年度_考試類型_來源卷別_模擬考
```

範例：

```text
高中物理_112_學測_112中2_模擬考
```

段考：

```text
課程_學年度學期_學校_年級_考試次別_段考
```

範例：

```text
選修化學1_115上_北大_高三_一段_段考
```

其他文件：

```text
日期_課程或主題_內容_說明
```

範例：

```text
260123_選修物理1_章節整理_修正版
```

### 共同規則

- 課程或主題放最前面；其他文件例外，日期放最前面。
- 一律使用底線 `_` 分隔。
- 章節固定兩位數，例如 `CH02`。
- 有序號的文件名稱固定兩位數，例如 `講義01`、`小考01`。
- `講義`、`考卷`、`段考`、`模擬考` 不放在檔名最前面。
- 自編教材的類別放在「文件名稱」欄位，例如 `講義01`、`小考01`、`題組02`。

### UI 行為

- 可切換檔名類型。
- 可從既有科目、課程 / 冊別、章節資料帶入。
- 可輸入各類型需要的欄位。
- 即時顯示檔名預覽。
- 可一鍵複製檔名。
- 不會自動搬移、批次重新命名或修改既有教材檔案。

### 已修改檔案

- `index.html`
- `app.js`

### 已驗證案例

```text
選修物理1_CH02_運動學_講義01_教用_草稿
選修物理1_112_學測_112中2_模擬考
選修化學1_115上_北大_高三_一段_段考
260123_選修物理1_章節整理_修正版
```

### 後續注意

- 若原始課程 / 冊別名稱包含出版社、學年或版本資訊，建議用「課程名稱微調」欄位調整成教材檔名要使用的短名稱。
- 若未來要批次改名既有檔案，應先做預覽清單與備份機制，不應直接改名。
